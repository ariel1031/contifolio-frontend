import styled from 'styled-components'

export const FormContainer = styled.form`
    //styled.원하는 HTML.
    // 여기서는 form 태그와 같은 기능 가지고, 아래의 스타일을 가진다.
    // form 태그를 쓰는거다. 거기에 스타일을 곁들인
    display: flex;
    flex-direction: column;
    background-color: aquamarine;
`
